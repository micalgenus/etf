version: "3"

services:
  backend:
    build:
      context: ./backend
      dockerfile: ./Dockerfile.dev
    ports:
      - 8080:8080
    volumes:
      - ./backend:/node/
      - gradle:/root/.gradle/

volumes:
  gradle:
