version: "3.7"

services:
  backend:
    build:
      context: ./backend
      dockerfile: ./Dockerfile.dev
    ports:
      - 8080:8080
      - 35729:35729
    volumes:
      - ./backend/:/spring/
      - gradle:/root/.gradle/

volumes:
  gradle: